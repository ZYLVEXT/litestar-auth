Deterministic Ed25519 private seed is bytes(range(32)). Private seed is for
vector regeneration only; consumers never receive signer private keys. Regenerate
with the same seed, now=2026-07-31T12:00:00+00:00, and lifetime=300s.
