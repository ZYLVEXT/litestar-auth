global:
  scrape_interval: 5s
  evaluation_interval: 5s

rule_files:
  - /etc/prometheus/recording-rules.yml

scrape_configs:
  - job_name: authweave-collector
    static_configs:
      - targets: ["collector:8889"]
        labels:
          pipeline: authweave-reference
